# 이 게임을 처음부터 어떻게 만드는가

이 문서는 완성된 코드를 "설명"하는 문서가 아니라, **여러분이 아무 것도 없는 빈 폴더에서 시작해서 이 게임을 직접 다시 만들 수 있도록**, 만든 순서 그대로 따라가며 "왜 이 순서로, 왜 이렇게" 만들었는지를 설명하는 문서입니다.

실제로 소프트웨어를 만들 때는 규칙(3단계)부터 다 정해놓고 코드를 한 번에 쭉 쓰지 않습니다. **작게 만들고, 눈으로 확인하고, 그 위에 한 층씩 쌓는 것**이 핵심입니다. 아래 순서가 바로 그 방식입니다.

---

## 0단계. 시작하기 전에 — 전체 그림 먼저 그리기

코드를 한 줄도 쓰기 전에, 이 시스템에 등장하는 것이 몇 개이고 서로 어떻게 연결되는지부터 종이에 그려봅니다.

```
[ESP32-S3]  ---- Wi-Fi(AP) ---->  [휴대폰 브라우저]  (조작기 = 컨트롤러)
    |                              [컴퓨터 브라우저]  (게임 화면 = 디스플레이)
    |
  전체 게임 상태를 여기서만 계산한다
  (위치, 방향, 술래 여부, 맵)
```

이 그림에서 가장 중요한 결정 하나가 나옵니다.

> **"위치와 방향 계산은 ESP32만 한다. 브라우저는 절대 계산하지 않는다."**

왜 이 결정이 제일 먼저 나와야 할까요? 만약 "휴대폰이 버튼을 누르면 브라우저가 몇 칸 이동했는지 계산해서 서버에 알려준다"는 식으로 만들면, 두 대의 휴대폰이 계산한 결과가 서로 어긋났을 때 누구 말이 맞는지 정할 방법이 없습니다. 그래서 **딱 한 곳(ESP32)만 "진짜 상태"를 가지고, 나머지는 전부 그 상태를 그대로 보여주기만 하는 구경꾼**으로 만드는 것이 이후의 모든 설계를 단순하게 만들어 줍니다. 이걸 **"서버 권위(server-authoritative) 구조"**라고 부릅니다. 앞으로 나올 결정 대부분이 이 원칙 하나에서 파생됩니다.

이 그림이 이해되면 준비가 끝난 것입니다.

---

## 1단계. 뼈대부터: "켜졌다"는 것만 확인하기

가장 먼저 할 일은 게임 로직이 아니라 **"보드에 코드가 올라가고 살아있다"**는 것을 눈으로 보는 것입니다.

1. PlatformIO로 빈 프로젝트를 만듭니다 (`platformio.ini`, `src/main.cpp`만 있는 상태).
2. `platformio.ini`에 보드(`esp32-s3-devkitc-1`)와 USB-CDC 플래그(`ARDUINO_USB_CDC_ON_BOOT`, `ARDUINO_USB_MODE`)를 넣습니다. 이게 없으면 나중에 시리얼 모니터에 아무것도 안 찍혀서 "보드가 죽었나?" 하고 몇 시간을 낭비하게 됩니다. **가장 먼저 해결해야 할 게 시리얼 출력**입니다. 눈이 없으면 그 뒤로는 전부 깜깜이로 개발하는 것과 같습니다.
3. `setup()`에서 `Serial.begin(115200)` 하고 `Serial.println("살아있음")` 정도만 찍고, `loop()`는 비워둡니다.
4. 업로드 → 시리얼 모니터 → 글자가 보이면 1단계 완료.

> **이 프로젝트에서는**: [src/main.cpp](src/main.cpp)의 `setup()` 맨 앞부분이 이 단계의 결과물입니다. `[INFO]`로 시작하는 로그를 남기는 습관도 여기서부터 만들어 둔 것입니다 (나중에 로그가 많아지면 종류별로 눈에 잘 띄어야 하므로).

---

## 2단계. Wi-Fi AP 켜기 — 휴대폰으로 신호가 잡히는지 확인

다음 층은 "ESP32가 자기만의 Wi-Fi를 만든다"입니다. 아직 웹서버도, 게임도 없이 **Wi-Fi AP만** 켭니다.

```cpp
WiFi.mode(WIFI_AP);
WiFi.softAP("ESP32-TAG-GAME", "12345678");
Serial.println(WiFi.softAPIP()); // 보통 192.168.4.1
```

업로드하고 휴대폰 Wi-Fi 목록에 `ESP32-TAG-GAME`이 뜨는지, 비밀번호를 넣고 연결이 되는지만 확인합니다. 아직 브라우저로 접속은 안 해도 됩니다. **한 번에 한 가지만 확인**하는 게 핵심입니다 — Wi-Fi도 안 잡히는데 웹서버까지 만들어놓으면, 문제가 생겼을 때 "Wi-Fi가 문제인지, 웹서버가 문제인지" 구분이 안 됩니다.

> **이 프로젝트에서는**: [include/Config.h](include/Config.h)에 SSID/비밀번호를 상수로 빼두고, [src/main.cpp](src/main.cpp)의 `WiFi.softAP(...)` 호출 부분이 이 단계입니다.

---

## 3단계. 정적 파일 서버 — 브라우저에 HTML 한 장 띄우기

이제 "브라우저로 뭔가 보이게" 만듭니다. 게임 로직 없이, `<h1>안녕</h1>` 한 줄짜리 HTML 파일 하나만 준비합니다.

1. `data/index.html`에 아주 간단한 HTML을 하나 넣습니다.
2. `platformio.ini`에 `board_build.filesystem = littlefs`를 추가합니다.
3. `main.cpp`에서 `LittleFS.begin(true)`로 파일시스템을 마운트하고, `AsyncWebServer`를 만들어 `serveStatic("/", LittleFS, "/")`로 `data` 폴더를 통째로 웹에 연결합니다.
4. **펌웨어 업로드와는 별개로** `pio run --target uploadfs`로 `data` 폴더 내용만 따로 올립니다. (이 "펌웨어 업로드"와 "파일시스템 업로드"가 서로 다른 두 단계라는 걸 여기서 몸으로 익혀두는 게 중요합니다 — 나중에 "코드를 고쳤는데 웹페이지가 그대로다"라는 삽질을 막아줍니다.)
5. 브라우저에서 `http://192.168.4.1` 접속 → 글자가 보이면 완료.

> **이 프로젝트에서는**: [data/index.html](data/index.html)이 이 단계에서 시작해서 점점 채널/이름 입력 폼이 붙은 지금 모습으로 자랐습니다.

---

## 4단계. WebSocket 연결만 — 아직 게임 없이 "핑퐁"만

HTTP(정적 파일)와 WebSocket(실시간 통신)은 다른 것입니다. 게임을 만들기 전에 **WebSocket 연결 자체가 되는지**부터 확인합니다.

서버 쪽:
```cpp
AsyncWebSocket ws("/ws");
ws.onEvent([](AsyncWebSocket*, AsyncWebSocketClient* client, AwsEventType type, void*, uint8_t*, size_t){
  if (type == WS_EVT_CONNECT) Serial.println("연결됨");
});
server.addHandler(&ws);
```

브라우저 콘솔 쪽:
```js
const ws = new WebSocket("ws://192.168.4.1/ws");
ws.onopen = () => console.log("연결됨");
```

이 둘만 맞춰보고 시리얼/콘솔에 "연결됨"이 뜨는지 확인합니다. **아직 JSON도, join도, 게임 로직도 없습니다.** 이 단계를 건너뛰고 바로 복잡한 메시지를 주고받으려 하면, 연결 자체가 안 되는 건지 메시지 처리 로직이 잘못된 건지 구분이 안 됩니다.

> **이 프로젝트에서는**: [lib/GameServer/GameServer.cpp](lib/GameServer/GameServer.cpp)의 `onEvent()` 함수 중 `WS_EVT_CONNECT`/`WS_EVT_DISCONNECT` 로그 부분이 이 단계의 흔적입니다.

---

## 5단계. 메시지 규격을 코드보다 먼저 "종이"에 정하기

여기서부터가 진짜 설계입니다. **코드를 쓰기 전에 클라이언트↔서버가 주고받을 JSON 메시지의 모양을 먼저 문서로 정합니다.** 코드보다 문서(또는 메모)가 먼저 나와야 하는 이유는, 프론트엔드(브라우저)와 백엔드(ESP32)를 동시에 만들 것이기 때문입니다. 규격이 없으면 둘이 서로 다른 모양의 JSON을 주고받다가 계속 어긋납니다.

이 프로젝트가 정한 규격 (이 순서로 하나씩 정해나갔습니다):

1. **모든 메시지는 `{"type": "..."}` 필드로 종류를 구분한다.** (다른 방식도 있지만, `type` 하나로 분기하는 게 가장 단순합니다.)
2. **클라이언트 → 서버**: `join`(참가), `turn`(회전), `startGame`(게임 시작), `requestState`(상태 재요청).
3. **서버 → 클라이언트**: `joined`(참가 확인), `state`(전체 상태), `error`(오류), `gameStarted`, `playerCaught`, `playerDisconnected`.
4. **회전 메시지에는 "누구를 회전시킬지" 정보를 아예 넣지 않는다.** `{"type":"turn","direction":"clockwise"}` 뿐입니다. 플레이어 ID를 클라이언트가 보내게 하면, 악의적인 클라이언트가 다른 사람 ID를 보내서 남을 조작할 수 있습니다. 그래서 "이 WebSocket 연결이 누구인지"는 **서버가 연결 시점에 기억해뒀다가** 그 정보만 사용합니다. (이게 8단계에서 나오는 `ConnectionInfo`로 이어집니다.)

이 표를 먼저 만들어두면, 프론트엔드 개발자와 백엔드 개발자가 (혼자 만들더라도!) 서로 딴 길로 새지 않습니다.

> **이 프로젝트에서는**: [include/WebSocketProtocol.h](include/WebSocketProtocol.h)가 이 "합의된 규격"을 코드 상수로 옮겨놓은 것입니다. 나중에 `"join"`이라고 여기저기 직접 타이핑하다가 오타 내는 사고를 막아줍니다.

---

## 6단계. 데이터 구조 설계 — "무엇을 기억해야 하는가"부터 나열하기

메시지 규격이 정해졌으면, 이제 **서버가 무엇을 기억하고 있어야 이 메시지들을 처리할 수 있는지**를 거꾸로 추론합니다.

- `join`을 처리하려면 → 이 채널이 이미 있는지 찾아야 함 → **채널 목록**이 필요하다
- 채널 안에는 → 누가 참가했는지, 어디 있는지, 어느 방향인지 → **플레이어 목록**이 필요하다
- `turn`을 처리하려면 → 이 WebSocket 연결이 어느 채널의 몇 번 플레이어인지 즉시 알아야 함 → **연결 하나하나에 대한 정보**가 필요하다

이렇게 거꾸로 따라가면 자연스럽게 세 가지 구조체가 나옵니다.

```cpp
struct Player { x, y, direction, isTagger, token, name, connected, ... };
struct GameRoom { channel, players[8], map[12][12], gameStarted, ... };
struct ConnectionInfo { clientId, roomIndex, playerIndex, isDisplay };
```

**왜 `String` 대신 고정 크기 배열(`char name[17]`)을 쓸까?** ESP32는 메모리가 넉넉하지 않고, `String`을 계속 만들었다 지웠다 하면 "메모리 조각남(fragmentation)"이라는 문제가 생겨서 오래 켜두면 이상하게 죽는 원인이 됩니다. 처음부터 "이름은 최대 16글자"처럼 한계를 정하고 고정 배열을 쓰면 이 문제 자체가 생기지 않습니다. **작은 임베디드 기기에서는 "얼마나 유연한가"보다 "얼마나 예측 가능한가"가 더 중요합니다.**

**왜 `ConnectionInfo`를 따로 둘까?** WebSocket 연결 자체(`AsyncWebSocketClient`)에는 "이건 A채널의 3번 플레이어다" 같은 정보를 안전하게 매달아 둘 자리가 마땅치 않습니다. 그래서 서버가 직접 `connections_[]` 배열을 만들어서 "연결 ID → 어느 방, 몇 번 플레이어인지"를 기억합니다. 이게 있어야 5단계에서 정한 "클라이언트가 자기 ID를 속일 수 없다"는 원칙이 실제로 지켜집니다.

> **이 프로젝트에서는**: [include/GameTypes.h](include/GameTypes.h) 전체가 이 단계의 결과물입니다. [include/Config.h](include/Config.h)에는 "최대 4채널, 채널당 최대 8명" 같은 한계값을 상수로 미리 빼두었습니다 — 이 상수들이 바로 위 구조체들의 배열 크기를 결정합니다.

---

## 7단계. join 로직 먼저 완성하기 — 다른 기능보다 먼저

메시지 종류 중에 `join`을 제일 먼저 완성해야 합니다. 나머지 모든 메시지(`turn`, `startGame`)는 "이미 참가한 사람"을 전제로 하기 때문입니다.

구현 순서 (실제로 짠 순서):
1. `role`이 `controller`인지 `display`인지 본다.
2. `channel` 문자열이 규칙(영문/숫자 1~16자)에 맞는지 검사한다. — **여기서부터 "클라이언트가 보낸 값은 일단 의심한다"는 습관이 시작됩니다.**
3. 그 채널이 이미 있으면 찾고, 없으면 (최대 4개 한도 안에서) 새로 만든다.
4. `display`면: 연결 정보만 등록하고 끝.
5. `controller`면: 토큰이 왔는지 먼저 본다 → **토큰이 유효하면 기존 플레이어로 복귀**(재접속), 아니면 **새 플레이어 생성**(빈 자리 찾기 → 시작 위치 찾기 → 토큰 발급).
6. 참가 결과(`joined`)와 현재 상태(`state`, 맵 포함)를 돌려준다.

이 시점에서 프론트엔드도 최소한으로 같이 만들어서, **참가 버튼을 눌렀을 때 서버 로그에 "새 플레이어 생성"이 찍히는 것**을 직접 눈으로 확인합니다. 아직 화면에 캔버스도, 회전 버튼 동작도 필요 없습니다.

> **이 프로젝트에서는**: [lib/GameServer/GameServer.cpp](lib/GameServer/GameServer.cpp)의 `handleJoin()` 함수가 바로 이 단계 전체입니다. 코드를 읽을 때 "컴퓨터 화면 참가" 블록과 "휴대폰 조작기 참가" 블록으로 나뉜 걸 확인해보면 위 순서와 그대로 대응됩니다.

---

## 8단계. turn 로직 — "서버 권위" 원칙을 실제로 지키는 첫 코드

`join`이 되면, 다음으로 제일 쉬운 `turn`을 구현합니다. 여기서 0단계에서 정한 원칙이 코드로 처음 나타납니다.

```cpp
void handleTurn(ConnectionInfo &conn) {
    // conn은 "이 WebSocket이 누구인지" 서버가 이미 알고 있는 정보.
    // 메시지 안에 들어있는 값이 아니라, join할 때 서버가 저장해둔 값을 쓴다.
    Player &p = rooms_[conn.roomIndex].players[conn.playerIndex];
    p.direction = rotateClockwise(p.direction); // 방향만 바꾼다. 위치는 안 바꾼다.
}
```

여기서 중요한 두 가지 결정:
- **회전 버튼 = "다음 이동 방향을 바꾸는 것"이지, "한 칸 이동시키는 것"이 아닙니다.** 실제 이동은 9단계의 게임 틱이 담당합니다. 입력(회전)과 시뮬레이션(이동)을 분리해두면, 나중에 "이동 속도를 바꾸고 싶다" 같은 요구가 와도 회전 로직은 전혀 안 건드려도 됩니다.
- **너무 빠른 입력 제한**(`MIN_TURN_INTERVAL_MS`)도 여기서 같이 넣습니다. 이유는 뒤로 미루지 않고 "이 기능을 만드는 시점"에 바로 넣는 게, 나중에 "버튼 연타하면 이상해진다"는 버그 리포트를 받고 다시 여기로 돌아오는 것보다 훨씬 쌉니다.

---

## 9단계. 게임 틱 — 이 프로젝트에서 가장 어려운 부분

`join`, `turn`이 되면 이제 "가만히 둬도 자동으로 움직이는" 부분을 만듭니다. 이게 가장 까다로운 이유는 **여러 플레이어가 동시에 움직이기 때문**입니다.

### 9-1. 왜 `delay()`를 쓰면 안 되는가
`delay(400)`으로 0.4초를 그냥 기다리면, 그 0.4초 동안 ESP32는 다른 어떤 일(WebSocket 메시지 처리, 웹서버 응답)도 못 합니다. 그래서 "지금이 몇 시인지"를 기억해뒀다가 시간이 지났는지 **매번 확인만 하고 지나가는** 방식을 씁니다.

```cpp
unsigned long now = millis();
if (now - room.lastTick >= TICK_INTERVAL_MS) {
    room.lastTick = now;
    // 이번에만 한 칸 이동 처리
}
```
이렇게 하면 `loop()`는 계속 빠르게 돌면서, "0.4초가 지났을 때만" 이동을 처리하고, 그 사이에도 다른 메시지들은 즉시즉시 처리됩니다.

### 9-2. 이동을 "한 명씩 바로 적용"하면 안 되는 이유
가장 순진한 방법은 이렇습니다: "플레이어 1을 먼저 옮기고, 그 자리에 플레이어 2가 못 들어오게 막고, 플레이어 3을 옮기고..." 이렇게 하면 **먼저 계산되는 사람이 유리해지는 게 매 틱마다 순서가 바뀌면 불공평**해지고, 디버깅도 어려워집니다.

그래서 이 순서를 딱 정해서 지킵니다 (실제로 이 순서 그대로 구현되어 있습니다):

1. **전원**의 "가고 싶은 칸"을 먼저 계산만 해둔다 (아직 아무도 움직이지 않음).
2. 벽/장애물 규칙으로 각자 이동 가능 여부를 정리한다.
3. 도망자끼리 같은 칸을 원하면, **배열 순서가 빠른 사람이 우선권**을 갖는다고 미리 정해둔다 (이 규칙 자체가 "정답"이라기보다, "누가 이겨도 상관없지만 규칙은 있어야 한다"는 게 핵심입니다).
4. 그제서야 전원의 위치를 한꺼번에 반영한다.
5. 그 다음에 술래-도망자가 겹쳤는지 검사한다.
6. 마지막으로 결과를 전송한다.

**"계산 따로, 반영 따로"**가 핵심 패턴입니다. 계산하면서 동시에 반영해버리면, 뒤에 계산되는 플레이어가 앞사람이 이미 바뀐 위치를 보고 판단하게 되어 결과가 매번 달라집니다.

> **이 프로젝트에서는**: [lib/GameServer/GameServer.cpp](lib/GameServer/GameServer.cpp)의 `updateRoomTick()` 함수에 1~6단계 주석이 코드 순서 그대로 달려 있습니다. 처음 이 함수를 짤 때도 실제로 이 6줄짜리 순서를 먼저 주석으로 써놓고, 그 아래에 한 블록씩 채워나갔습니다. **복잡한 함수를 짤 때는 먼저 "단계 이름만" 주석으로 나열하고, 그다음 한 단계씩 코드를 채우는 방식**을 추천합니다.

---

## 10단계. 상태를 클라이언트에 어떻게 보여줄지 정하기

서버 안의 상태가 바뀌어도, 브라우저에 전송하지 않으면 아무도 모릅니다. 여기서 정해야 할 질문: **"얼마나 자주, 얼마나 많이 보낼 것인가?"**

이 프로젝트가 내린 결정과 이유:
- **맵(12×12=144칸)은 방에 처음 참가할 때 딱 1번만 보낸다.** 맵은 게임 중에 절대 안 바뀌기 때문에, 매 0.4초마다 144개 숫자를 반복 전송하는 건 낭비입니다.
- **그 이후에는 플레이어 정보(위치/방향/술래 여부)만 보낸다.** 이게 훨씬 작고, 자주 바뀌는 값만 자주 보내는 것이 맞습니다.
- **"바뀔 때만 보낸다"가 아니라 "매 틱마다 보낸다"를 선택했습니다.** 왜냐하면 8명이 있으면 어차피 거의 매 틱마다 누군가는 움직이기 때문에, "바뀐 사람만 골라서 보내기"는 코드만 복잡해지고 실제 이득은 크지 않았기 때문입니다. **최적화는 필요한 만큼만** 하는 것이 좋습니다.

> **이 프로젝트에서는**: [lib/GameServer/GameServer.cpp](lib/GameServer/GameServer.cpp)의 `buildStateJson()` 위에 이 결정과 이유를 그대로 주석으로 남겨뒀습니다. 나중에 "왜 이렇게 만들었더라?"를 스스로 잊어버리지 않기 위해서라도, 결정한 이유는 코드 옆에 적어두는 습관을 들이는 게 좋습니다.

---

## 11단계. 잡기 판정 — 규칙을 코드로 옮기기

"술래와 도망자가 같은 칸에 있으면 잡힌다"는 규칙 자체는 쉽습니다. 어려운 건 **"동시에 여러 명이 잡힐 수 있는 상황을 어떻게 처리할 것인가"**입니다. 여기서도 9단계와 같은 패턴을 다시 씁니다: **"배열 순서상 첫 번째만 처리하고 나머지는 다음 틱으로 미룬다"**는 규칙을 미리 정하고, `break`로 한 번만 처리한 뒤 반복문을 빠져나옵니다. 규칙이 없으면 "동시에 두 명이 잡히면 술래가 2명이 되는" 이상한 상태에 빠질 수 있습니다.

---

## 12단계. 연결이 끊기는 상황들 대비하기

여기까지 하면 "정상적으로 잘 되는 경우"는 다 됩니다. 이제 **비정상적인 상황**들을 하나씩 나열하고 대비합니다. (이 목록을 먼저 종이에 적어보는 게 좋습니다.)

- 와이파이가 끊기면? → 브라우저가 자동으로 재연결을 시도해야 한다 (프론트엔드 `common.js`의 `createGameSocket`).
- 브라우저를 새로고침하면? → 새 플레이어가 또 생기면 안 된다 → **토큰**을 `localStorage`에 저장해뒀다가 재접속 시 같이 보낸다.
- 술래가 연결을 끊고 안 돌아오면? → 게임이 영원히 멈추면 안 된다 → 일정 시간(`PLAYER_TIMEOUT_MS`) 후 제거하고, 술래였다면 **새 술래를 다시 뽑는다**.
- 아무도 없는 채널을 계속 붙잡고 있으면? → 메모리 낭비 → 오래 비어있으면 채널 자체를 정리한다.

이런 "예외 상황 목록"은 기능을 다 만들고 나서 마지막에 몰아서 하지 말고, **7~11단계를 만드는 동안에도 계속 옆에 메모해뒀다가**, 정상 동작이 끝난 뒤 이 단계에서 한꺼번에 처리하는 게 효율적입니다. (동시에 만들려고 하면 "정상 흐름이 뭔지"조차 헷갈립니다.)

> **이 프로젝트에서는**: `handleDisconnect()`와 `cleanupRooms()`가 이 단계의 결과물입니다.

---

## 13단계. 마지막 퍼즐 조각: 동시성(race condition) 문제

9단계까지는 "loop() 안에서 순서대로 실행된다"고 가정하고 만들었습니다. 그런데 실제로는:

- 게임 틱(9단계)은 `loop()`(메인 태스크)에서 실행됩니다.
- WebSocket 메시지 처리(7, 8단계)는 **AsyncTCP 라이브러리가 만든 별도의 태스크**에서 실행됩니다.

이 둘이 **동시에** 같은 배열(`rooms_`)을 건드릴 수 있습니다. 예를 들어 게임 틱이 플레이어 위치를 반영하는 도중에, 하필 그 순간 `turn` 메시지가 도착해서 같은 플레이어의 방향을 동시에 바꾸면 어떻게 될까요? 최악의 경우 데이터가 반쯤만 바뀐 이상한 상태가 될 수 있습니다.

해결 방법은 "동시에 둘 다 못 들어오게 문을 잠그는 것"입니다. FreeRTOS의 뮤텍스(mutex, 상호 배제 락)를 씁니다.

```cpp
xSemaphoreTakeRecursive(mutex_, portMAX_DELAY); // 문 잠그기
// ... rooms_, connections_ 를 읽거나 쓰는 코드 ...
xSemaphoreGiveRecursive(mutex_);                 // 문 열기
```

**이 단계를 맨 마지막에 넣는 이유**: 동시성 문제는 눈에 잘 안 보이고, 어쩌다 한 번 이상하게 동작하는 식으로만 드러나서 처음부터 신경 쓰면서 코드를 짜면 오히려 더 헷갈립니다. 먼저 "혼자 실행된다"고 가정하고 로직을 다 맞춘 다음, **"이 코드가 동시에 두 곳에서 실행될 수 있는 지점이 어디인가"를 나중에 찾아서 잠그는 것**이 실수를 줄이는 순서입니다.

> **이 프로젝트에서는**: [lib/GameServer/GameServer.h](lib/GameServer/GameServer.h)의 `mutex_`, 그리고 `GameServer.cpp`의 `handleMessage()`/`handleDisconnect()`/`loop()` 맨 앞뒤에 있는 lock/unlock 쌍이 이 단계입니다.

---

## 14단계. 입력 검증 — "클라이언트를 믿지 않는다"를 끝까지 적용

0단계의 "서버 권위" 원칙을 통신 입력값에도 끝까지 적용하는 단계입니다. 클라이언트가 보내는 모든 값은 다음 순서로 의심합니다.

1. JSON 자체가 깨져 있을 수 있다 → `deserializeJson()`의 반환값(에러)을 반드시 확인한다.
2. 있어야 할 필드가 없을 수 있다 → `obj["field"] | "기본값"` 형태로 항상 기본값을 준비해둔다.
3. 문자열이 너무 길 수 있다 → 고정 배열에 복사하기 전에 길이를 확인한다 (버퍼 초과 방지).
4. 값이 허용된 범위를 벗어날 수 있다 → 채널 이름은 영문/숫자만, 역할은 `controller`/`display`만 허용.
5. 순서가 이상할 수 있다 → `join` 하기 전에 `turn`을 보내면? → 등록된 연결 정보가 없으므로 거부.

이 검증들을 기능 단위(7~11단계)로 만들 때 바로바로 넣지 않고 굳이 마지막에 정리하는 이유는, **"이 값이 어떻게 잘못될 수 있는가"는 기능이 다 만들어져야 전체적으로 보이기 때문**입니다. 물론 실제로 개발할 때는 이 프로젝트도 각 단계를 만들면서 검증을 같이 넣었습니다 — 여기서는 "검증이라는 사고방식 자체"를 마지막 단계로 정리해서 보여드리는 것입니다.

---

## 15단계. 통합 테스트

기능이 다 만들어지면, 마지막으로 실제 시나리오를 순서대로 밟아봅니다 (자세한 체크리스트는 프로젝트의 테스트 절차 참고). 이 순서로 확인하면 좋습니다.

1. 혼자 참가해서 화면에 나타나는지
2. 두 번째 기기로 같은 채널에 참가해서 둘 다 보이는지
3. 게임 시작 → 자동으로 움직이는지
4. 회전 버튼 → 다음 이동부터 방향이 바뀌는지
5. 벽/장애물에 막히는지
6. 잡히면 역할이 바뀌는지
7. 다른 채널을 만들어서 서로 안 섞이는지
8. 새로고침해도 같은 플레이어로 돌아오는지

---

## 정리: 처음부터 다시 만든다면 이 순서로

1. 시리얼 출력 확인 (뼈대)
2. Wi-Fi AP 확인
3. 정적 파일(HTML 한 장) 서빙 확인
4. WebSocket 연결만 확인
5. 메시지 규격을 문서로 먼저 정하기
6. 데이터 구조 설계 (무엇을 기억해야 하는가 거꾸로 추론)
7. join 구현 + 프론트엔드 최소 연결
8. turn 구현 (서버 권위 원칙 적용)
9. 게임 틱 구현 (계산 따로 → 반영 따로 → 판정)
10. 상태 전송 정책 정하기 (얼마나 자주, 얼마나 많이)
11. 잡기 판정 + 역할 교체
12. 예외 상황(연결 끊김/재접속/빈 방 정리) 목록화 후 처리
13. 동시성 문제 찾아서 뮤텍스로 보호
14. 입력 검증을 전체적으로 다시 점검
15. 시나리오 기반 통합 테스트

이 순서의 공통점은 **"항상 눈으로 확인할 수 있는 가장 작은 조각부터 만들고, 그 위에 한 층씩 쌓는다"**는 것입니다. 게임 규칙이 아무리 복잡해 보여도, 결국 이렇게 잘게 쪼개서 하나씩 눈으로 확인하며 올라가면 어느 순간 완성되어 있습니다. 다음에 새로운 기능(예: 라운드 제한 시간)을 추가할 때도 이 15단계와 같은 방식 — "무엇을 기억해야 하는지 → 메시지 규격 → 서버 로직 → 화면 반영 → 예외 처리" — 을 그대로 반복하면 됩니다.
